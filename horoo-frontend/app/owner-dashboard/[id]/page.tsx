"use client";

import Link from "next/link";
import { ArrowLeft, BedDouble, Check, Home, MapPin, Pencil, Ruler, Users } from "lucide-react";
import { useEffect, useState } from "react";
import { getOwnerListing } from "@/lib/ownerListings";
import type { ApiListing } from "@/lib/listings";
import ListingGallery from "@/components/listings/ListingGallery";

export default function OwnerListingPage({ params }: { params: Promise<{ id: string }> }) {
  const [listing, setListing] = useState<ApiListing | null>(null);
  const [error, setError] = useState("");
  useEffect(() => { params.then(({ id }) => getOwnerListing(id).then((result) => setListing(result.listing)).catch((requestError) => setError(requestError instanceof Error ? requestError.message : "Listing not found"))); }, [params]);
  if (error) return <main className="mx-auto max-w-6xl px-4 py-6 text-sm text-red-700">{error}</main>;
  if (!listing) return <main className="mx-auto max-w-6xl px-4 py-6 text-sm text-gray-600">Loading listing...</main>;
  const title = listing.name || listing.title || "Untitled listing";
  const location = [listing.area, listing.city, listing.state].filter((value) => typeof value === "string" && value).join(", ");
  const listingTypes = [listing.flatType, listing.roomType].flatMap((value) => Array.isArray(value) ? value : value ? [value] : []).filter(Boolean);
  return <main className="min-h-screen bg-gray-50 px-4 py-8 sm:px-6 lg:px-8"><div className="mx-auto max-w-6xl"><div className="mb-5 flex items-center justify-between"><Link href="/owner-dashboard" className="inline-flex items-center gap-2 text-sm font-semibold text-gray-600 hover:text-orange-600"><ArrowLeft className="h-4 w-4" />Dashboard</Link><Link href={`/owner-dashboard/${listing._id}/edit`} className="inline-flex items-center gap-2 bg-orange-600 px-4 py-2 text-sm font-bold text-white hover:bg-orange-700"><Pencil className="h-4 w-4" />Edit listing</Link></div><div className="grid gap-4 lg:grid-cols-[minmax(0,1.5fr)_minmax(280px,0.8fr)]"><ListingGallery images={listing.images || []} title={title} /><section className="border border-gray-200 bg-white p-4 sm:p-5"><p className="text-xs font-bold uppercase tracking-wide text-orange-600">{listing.type}</p><div className="mt-1 flex items-start justify-between gap-3"><h1 className="text-2xl font-bold leading-tight text-gray-950">{title}</h1><p className="shrink-0 text-right text-xl font-bold text-orange-600">Rs. {listing.price ?? listing.rent ?? "-"}<span className="block text-xs font-normal text-gray-500">per month</span></p></div>{(location || listing.address) && <p className="mt-4 flex items-start gap-2 border-b border-gray-100 pb-4 text-sm text-gray-600"><MapPin className="mt-0.5 h-4 w-4 shrink-0 text-orange-500" /><span>{location || listing.address}{listing.pincode ? ` - ${listing.pincode}` : ""}</span></p>}<div className="mt-4 grid grid-cols-2 gap-2 text-sm text-gray-700">{listingTypes.length > 0 && <DetailItem icon={<BedDouble />} label={listingTypes.join(", ")} />}{listing.size && <DetailItem icon={<Ruler />} label={listing.size} />}{listing.availableFor?.length ? <DetailItem icon={<Users />} label={listing.availableFor.join(", ")} /> : null}<DetailItem icon={<Home />} label={listing.isAvailable === false ? "Not available" : "Available"} /></div></section></div><div className="mt-4 grid gap-4 lg:grid-cols-[minmax(0,1.5fr)_minmax(280px,0.8fr)]"><div className="space-y-4"><section className="border border-gray-200 bg-white p-4 sm:p-5"><h2 className="mb-2 text-base font-bold text-gray-900">About this place</h2><p className="whitespace-pre-wrap text-sm leading-6 text-gray-600">{listing.description || "No description added."}</p></section>{listing.facilities?.length ? <section className="border border-gray-200 bg-white p-4 sm:p-5"><h2 className="mb-3 text-base font-bold text-gray-900">Facilities</h2><div className="grid grid-cols-2 gap-2 sm:grid-cols-3">{listing.facilities.map((facility) => <div key={facility} className="flex items-center gap-2 text-sm text-gray-700"><Check className="h-4 w-4 text-green-600" />{facility}</div>)}</div></section> : null}{listing.address && <section className="border border-gray-200 bg-white p-4 sm:p-5"><h2 className="mb-2 text-base font-bold text-gray-900">Full address</h2><p className="text-sm text-gray-600">{listing.address}</p></section>}</div></div></div></main>;
}

function DetailItem({ icon, label }: { icon: React.ReactNode; label: string }) { return <div className="flex min-w-0 items-center gap-2 border border-gray-100 px-2 py-2 text-sm"><span className="text-orange-500 [&>svg]:h-4 [&>svg]:w-4">{icon}</span><span className="truncate capitalize">{label}</span></div>; }